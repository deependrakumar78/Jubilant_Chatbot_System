# Jubilant Chatbot System
A simple healthcare chatbot demo built with Flask.

## How to run locally
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python app.py`
3. Open http://127.0.0.1:5000 in your browser.
